services:
  backend:
    build: ./backend
    container_name: lunar-run-backend
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: lunar-run-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
